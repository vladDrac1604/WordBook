import { RemovexmltagsPipe } from './removexmltags.pipe';

describe('RemovexmltagsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovexmltagsPipe();
    expect(pipe).toBeTruthy();
  });
});
